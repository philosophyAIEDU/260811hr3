# Node.js
node_modules/
.next/
out/
build/
npm-debug.log*

# 환경 변수 / 로컬 설정 (API 키 등 민감 정보 포함 가능)
.env
.env.local
.env.*.local

# 로컬 전용 설정 파일
*.local.json

# Netlify CLI 로컬 캐시/링크 정보
.netlify/

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/
tsconfig.tsbuildinfo

# 로컬에서 npm run dev로 실행할 때 생기는 데이터 파일 (실제 배포에는 쓰이지 않음)
.data/
