# 그로우패스 실행 방법 (Netlify 배포 가이드)

컴퓨터에 아무것도 설치하지 않아도 되는, **Netlify 웹사이트에서 클릭만으로 배포하는 방법**을
기준으로 안내합니다. (회사 코드 저장소는 GitHub의 `philosophyAIEDU/260811hr3` 저장소이고,
개발 내용은 `claude/growthpath-app-dev-bzt98n` 브랜치에 있습니다)

데이터 저장은 **Netlify Blobs**를 쓰는데, 이건 Netlify에 배포하면 별도 연결 작업 없이
자동으로 동작합니다. (예전에는 데이터베이스를 따로 연결해야 했지만, MVP 단계에서는
그 과정을 없앴습니다 — 아래 "한계" 참고)

---

## 1. Netlify 사이트 만들기 (최초 1회)

1. https://app.netlify.com 에서 회사 계정으로 로그인(또는 가입)합니다.
2. 왼쪽 위 **"Add new site" → "Import an existing project"** 를 누릅니다.
3. **GitHub**를 선택하고, `philosophyAIEDU/260811hr3` 저장소를 찾아 선택합니다.
   (처음이면 Netlify가 GitHub 접근 권한을 요청합니다 — 승인해 주세요)
4. 배포 설정 화면에서 **"Branch to deploy"(배포할 브랜치)** 항목을 반드시
   `claude/growthpath-app-dev-bzt98n` 로 지정해 주세요.
   (기본값은 보통 `main`인데, 지금까지 만든 코드는 이 브랜치에만 있어서
   여기를 안 바꾸면 사이트가 비어 있거나 **"Page not found"(404) 오류**가 납니다)
5. Build command / Publish directory는 저장소의 `netlify.toml`이 자동으로 처리하므로
   비워두거나 기본값 그대로 두면 됩니다.
6. **"Deploy"** 버튼을 누릅니다. 몇 분 뒤 `https://이상한이름.netlify.app` 같은 주소가 생깁니다.
   (Netlify의 **Site settings → Change site name**에서 원하는 이름으로 바꿀 수 있습니다)
7. 배포가 끝나면(Deploys 탭에 초록색 "Published") 바로 접속해서 쓸 수 있습니다.
   **데이터베이스를 따로 연결하는 과정이 없습니다.**

> **지금 "Page not found"가 떴다면** 이 4번 단계(배포 브랜치)를 가장 먼저 확인해 주세요.
> Netlify 대시보드 → 해당 사이트 → **Site configuration → Build & deploy → Branches** 에서
> Production branch가 `claude/growthpath-app-dev-bzt98n`으로 되어 있는지 확인하고,
> 아니라면 바꾼 뒤 **Deploys 탭 → Trigger deploy**로 다시 배포해 주세요.

---

## 2. 관리자 코드 확인/변경 (선택)

HR 관리자 화면(`/admin`)에 들어갈 때 쓰는 코드입니다. 기본값은 `1234`입니다.

- 바꾸고 싶으면: Netlify 사이트 → **Site configuration → Environment variables** →
  **Add a variable** → 이름 `ADMIN_CODE`, 값에 원하는 코드를 입력 → 저장 →
  Deploys 탭에서 다시 배포합니다.

---

## 3. 접속해서 확인하기

1. 사이트 주소로 들어갑니다.
2. 시작 화면에서 사번(예: `E2024001`)과 이름(예: `김하늘`)을 입력해 봅니다.
   (아래 4번에서 샘플 데이터를 넣으면 이 사번으로 바로 테스트할 수 있습니다)
3. 처음 보는 사번을 입력하면 부서/직무 등록 화면이 나타납니다 — 등록 후 진단을 진행해 보세요.

---

## 4. 가상 샘플 데이터 10명 넣기 (선택, 테스트용)

빈 화면 대신 미리 채워진 예시 데이터로 화면을 확인하고 싶다면, 컴퓨터에서 아래처럼
한 번만 실행하면 됩니다. (실제 직원 정보가 아닌 가상 인물 10명입니다)

1. 컴퓨터에 Node.js가 설치되어 있어야 합니다 (https://nodejs.org 에서 설치, LTS 버전 권장).
2. 터미널(명령 프롬프트)에서 이 저장소 폴더로 이동한 뒤:
   ```
   npm install
   npm install -g netlify-cli
   netlify link            # 방금 만든 Netlify 사이트를 선택해 연결
   netlify dev:exec npm run db:seed
   ```
3. "샘플 데이터 시딩이 끝났습니다"가 뜨면 완료입니다. 사이트에서 `E2024001` (김하늘) 등으로
   접속해 결과를 확인해 보세요.

이 단계가 어렵게 느껴지면 생략해도 됩니다 — 앱은 실제 사번을 하나씩 등록해도 정상 동작합니다.

---

## 5. AI 맞춤 추천을 쓰려면 (Gemini API 키)

1. 사이트의 **"설정"** 메뉴로 들어갑니다.
2. "공용 API 키(관리자 전용)" 영역에 관리자 코드와 Google AI Studio에서 발급받은
   Gemini API 키를 입력하고 저장합니다. (키 발급: https://aistudio.google.com/apikey)
3. 이후 전 직원이 "AI 맞춤 추천" 화면에서 이 공용 키로 추천을 받을 수 있습니다.
   개인이 자기 키를 쓰고 싶으면 "설정" 화면의 "개인 API 키" 영역에 따로 입력하면 됩니다
   (이 개인 키는 그 사람 브라우저에만 저장되고 서버에는 저장되지 않습니다).

---

## 데이터 저장 방식의 한계 (MVP 단계)

지금은 정식 데이터베이스 대신 Netlify Blobs(JSON 문서 하나)에 전체 데이터를 저장합니다.
"연결 설정 없이 바로 동작"이라는 장점이 있지만, **아주 짧은 시간에 여러 명이 동시에
데이터를 쓰면** 마지막에 저장된 내용이 이전 내용을 덮어쓸 수 있는 한계가 있습니다.
사내 소규모 인원이 테스트/시범 운영하는 단계에서는 문제가 될 가능성이 낮지만, 정식으로
많은 인원이 함께 쓰게 되면 정식 데이터베이스(Netlify DB 등)로 옮기는 것을 권장합니다.

---

## 자주 발생하는 문제

| 증상 | 원인 · 해결 |
|---|---|
| 사이트 주소로 들어가면 **"Page not found"(404)** | 십중팔구 **배포 브랜치가 `claude/growthpath-app-dev-bzt98n`가 아님**. 1번 항목의 경고 박스 참고. |
| 사번 확인 시 **"데이터를 불러오는 중 문제가 발생했습니다"** | Netlify에 정상 배포되지 않았을 가능성이 큽니다. Deploys 탭에서 최신 배포가 "Published"(성공)인지, Deploy log에 오류가 없는지 확인해 주세요. |
| AI 추천 만들기가 **"API 키를 먼저 등록해 주세요"** | 5번 항목대로 설정 화면에서 공용 또는 개인 API 키를 등록해야 합니다. |
| `/admin` 관리자 코드가 안 맞음 | 기본값은 `1234`입니다. 바꿨다면 Netlify 환경변수 `ADMIN_CODE` 값을 확인해 주세요. |

위 표로 해결되지 않으면, **어떤 화면에서 어떤 메시지가 떴는지** 알려주시면 바로 봐드리겠습니다.
