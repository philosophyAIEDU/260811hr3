# 이 파일은 예시입니다. 실제 값은 .env.local 파일에 넣고, 그 파일은 git에 올리지 않습니다.
# Netlify에 DB를 연결하면 NETLIFY_DATABASE_URL은 Netlify가 자동으로 넣어줍니다.
# (로컬에서 `netlify dev`로 실행할 때도 자동으로 값이 채워집니다 — 직접 입력할 필요 없음)
NETLIFY_DATABASE_URL=

# HR 관리자 화면(/admin) 진입 코드 (기본값 1234, 비워두면 기본값 사용)
ADMIN_CODE=1234
